<% provide(:title, 'About Wen') %>

<%
def dispIfParam
	param = params[:a]
	if param != nil
		return fib(param.to_i)
	end
	return 'param nil'
end

def fib n 
	if n > 30
		return 1 #avoid excessive computations
	end
	if n < 3
		return 1
	end
	return (fib(n-1) + fib(n-2))
end
%>

<h2>Schedumator Terminal >></h2>
<p>
  The <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>
  is a project to make a book and screencasts to teach web development
  with <a href="http://rubyonrails.org/">Ruby on Rails</a>. This
  is the sample application for the tutorial.
</p>
<p>
This is another paragraph.  DispIfParam: <%= dispIfParam %>
<% params[:b].to_i.times do %>
	<h3><%= "FOR HELVETICAAAAAAAA" %></h3>
<% end %>
</p>
